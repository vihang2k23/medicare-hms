# Optional: override JSON Server base URL (default http://localhost:3001)
# VITE_JSON_SERVER_URL=http://localhost:3001
# On Render: set this on the *static* service to your API URL, e.g. https://your-api.onrender.com (no trailing slash), then redeploy.
